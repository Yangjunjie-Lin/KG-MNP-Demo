# Application Phase 01 golden cases

Each directory contains a canonical registered-query request and a deterministic
expected semantic fingerprint. Runtime metadata is intentionally excluded. These
goldens use only the verified GraphDB publication lineage represented by the
tracked Foundation scenarios; they are not an alternate business database.
