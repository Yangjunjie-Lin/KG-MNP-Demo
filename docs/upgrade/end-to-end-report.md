# 本地整链验证与判定范围

后续原模型/多模态追加验收见 [原模型与多模态实测](original-models-multimodal.md)。下文保留先前替代 API 升级轮次的原始结果，不将其当作新增模型能力证明。

本报告不将旧修订 PASS、演示图片或合成测试身份当作正式发布证据。
准确命令、源码摘要和最终分区结果以同目录 `verification.json` 为准。

## 本轮最终结果

- 后端完整收集与执行 1919 项：八路并发首跑为 1910 PASS、1 个 OWL TIMEOUT、8 项平台跳过；该超时用例在相同源码、原限时下独立复测 PASS。最终覆盖为 1911 PASS、8 SKIP，不将首跑重写为 PASS。
- 前端 39 项测试、lint、类型检查和构建通过。
- 最新完整真实浏览器运行 8 PASS、0 FAIL、2 个仅适用于已完成工作区的条件跳过；两个条件用例另有真实 PASS。完整运行与服务器/Worker/凭证清理均通过。
- 最终源码摘要为 `3648e9ea6a1f3513c2ea69a312c32ef3dd79eecc591bdde686a14668f570db3b`。后端全量后仅修改四个验证/演示运行文件，应用库与 workbench/src 未变；受影响检查已重跑，差异证明见 evidence/final-source-comparison.json。
- 最新全套浏览器证据：runtime_logs/p09/browser-runs/managed-c673298e69db468f8e549cf750d6995a/managed-receipt.json。
- Wheel、SDist 重建一致性及两套全新非 editable 安装均 PASS；旧包、独立 Worker、授权下载和重启恢复通过。
- 最终应用源码上真实模型局部修复、重新检查/审核、精确答案编译及导出包重读验证通过；实际范围建议 API 也已执行，但不授予审批。

此前的整套失败、认证恢复测试遗漏和退出超时记录均保留。浏览器恢复修正不延长会话、不重放审批；退出修正只约束演示/验证服务器的残留读取和清理，不放宽建模语义检查。

2026-09-10 的历史冻结后端：1888 项收集与执行完全对应，1880 通过、8 项明确平台跳过，0 失败/错误；
源码前后摘要一致（`ca29dbb8ba3c0e63f3d8d7233945b1eb07eeb3bf6b272b23cc27c99d8d3ec1b3`）。
前端 39 项测试、类型检查、lint 与构建通过。早期整套浏览器运行保留 FAIL，
修复后的最小完整版本/回滚、新工作台和 MNP 读取分别复测通过，不重写早期失败为全套 PASS。

## 2026-09-11：已有 API 的显式替代验证

服务器已接通现有 OpenAI-compatible 网关，观察到模型 gpt-5.3-codex-spark。
保留配置别名与实际响应模型 ID，不声称取得了模型权重版本证明。
使用 LLM 检索替代、Unicode 分块替代；这些调用不计为 BGE/FAISS/tokenizer 推理。

开发期真实调用已完成：

- HR：两轮复用/补全加三段文本抽取，共五次 LIVE 调用；随后完整语义检查、逐项测试审核、精确答案编译、导出、Registry、对象查询和来源回查通过。无须新增结构时第二轮返回空 additions，不人为制造新术语。
- 林业：两轮 LIVE 建模建议后，完整本地工单、幂等、受控反馈更新、下一轮执行和回滚通过；此例输入为 CSV，不把它计为在线自由文本抽取。
- 零业务基线局部修复：显式故障注入将属性值改为 FaultInjectedValue，LIVE 模型依据源单元格提出 Alpha；服务器核验引文，产生新提案/审核队列，执行语义重验、重新人工审核和精确答案编译。

开发期证据位于 runtime_reports/live-assistance-*，每份注明受测源码及 source_unchanged。
开发期间源码变化的运行不冒充最终同快照回归；最终结果以 verification.json 为准。
失败尝试也保留，包括越界 IRI、引文定位拒绝，以及将普通“待人工审核”状态误送入修复上下文导致的冻结阻断。
后者已通过分离流程状态/实际缺陷修正；人工审核和真实规则/证据问题仍由原门禁强制执行。
最近一次语义诊断须与服务器提交收据摘要一致，不能用篡改的“通过”报告引导修复。

## HR：真实文件到正式交付包

输入为两份实际 CSV 和一份原文 TXT，沿用员工编号和部门编号；D-02 为运维部。
实际走 Source/Batch/Plan/规则化、严格建模会话、范围审核、结构准备、记录/文本映射、
共享语义检查、逐项测试角色审核、真实确认包、精确答案编译、导出与 Registry 导入。

已通过的工程验证包括：授权快照下载、下载文件摘要与大小比对、独立解包复验、
对象查询、原文与记录双源回查。可下载的真实包为 `evidence/hr-delivery.kgop`。

从这个包的 `data/abox.ttl` 重读得到：

- 5 个对象。
- 18 条业务三元组。
- 5 条系统 NamedIndividual 类型元数据，共 23 条 ABox 三元组。
- 精确答案：E-001→D-01，E-002→D-02，E-003→D-01。
- 正式来源清单中业务事实有 **24 个来源支持关联、40 个细粒度 Evidence 关联**。

最后一项与教材的 21 不相同：正式映射器还保留了三条“员工类型”事实的文本支持。
没有删除正确证据凑出 21，也不将 24 误标为 21。教材 21 的参考断言仍保留；
正式包与原教材在支持关联口径上的差异是明确记录的剩余一致性项。

## 林业：真实业务状态与反馈

三个古树、三个巡查、两个区域是合成工程数据，不是实地林业数据或诊断规则。
类、谓词和精确状态字面值进入锁定的 Domain Pack，通用工作台与执行核心不写死 Tree/Employee。
工单自然键为项目+对象+巡查。

服务级整链已经实际验证：

- GT-001 创建一张持久化工单，并重读核对；GT-002 不建单；GT-003 进入待核实。
- 重复执行返回 EXISTING，仍只有一张工单。
- 未验证/未审核的反馈候选不能激活。
- 优先级候选通过回归和测试角色审核后激活；旧计划被拒绝，下一轮新计划与执行收据采用 high 配置。
- 已存在工单不回溯改写，且不重复创建；配置影响新计划，不能把这一点说成既有工单优先级被修改。
- 回滚后新的计划重新采用 normal 配置。

0.1.1 实际下载包重读为 8 个对象、30 条业务三元组，另有 8 条系统 NamedIndividual 元数据；
不是 HR 的 5/18 计数。包及字节/图计数记录见 `evidence/forestry-delivery.kgop` 与 `evidence/package-counts.json`。

早期实测使用 `forestry-workorders@0.1.0`。该版本的实际 CSV 是 3/3/2，
但来源声明描述误沿用了原 forestry 示例的 6 条计数；修正版本为 **0.1.1**。
两个版本分开锁定，早期已审快照未被覆盖。最终新项目与新验证选择 0.1.1。

## 零业务基线的新建

`tests/upgrade/test_no_baseline.py` 从 empty 领域包及实际 CSV 开始，确认 baseline.elements 为空。
显式人工类/属性/实例/约束草案进入同一 Provider、候选集、联合检查、人工审核和编译器，
精确答案测试已通过。预校验返回 REVIEW_REQUIRED，而不是把结构有效当作自动批准。
原路径是人工设计加确定性执行；新增 LIVE 修复路径使用同一草案、重验、审核与编译核心，不代表从零自动理解任意领域规则。

## 安全与版本负向证据

四模块使用实际已提交运行执行 `module.evaluate`，工程检查均通过，报告保留在 `evidence/module-*.json`：
接入为 33 个 KG-IR Item，本体交付为 56 个包内文件，协同/执行为 3 个业务对象，反馈为 3 个回归决策。
这些分母不混作对象识别准确率；四份报告的研究结论仍是 INSUFFICIENT_EVIDENCE，未提供虚构金标准或专家指标。

新增检查覆盖：旧命名空间与新命名空间同一服务类/资源，环境变量冲突不回显密钥，
依赖变化 STALE、同输入不失效、旧审核拒绝、必需门 FAIL/NOT_RUN/ERROR/TIMEOUT/UNSUPPORTED 不放行，
审核头变化使旧确认/编译结果失效、伪造 APPROVED 文件缺少服务器提交凭据时拒绝、
真实 HTTP 会话到期 401、重新认证后原项目仍可读取且不重放写入、权限撤销仍拒绝。
既有跨项目/CSRF/Origin/受控下载/Worker fencing 测试继续保留。

## 浏览器与安装

五阶段新工作台测试启动真实 API/Worker，执行实际文件规则化和画像，无核心 API Mock。
1440×900、1366×768 和 390px 宽度做布局和 axe 检查。未运行阶段截图明确保持未运行，
不能用这组导航截图证明所有后台阶段已执行。
旧安全浏览器测试保留 CSP 外联阻断、脚本惰性数据、键盘焦点、跨项目与身份缓存隔离检查。
混合资料浏览器流程已通过下载复验、本地发布和对象读取；早期无障碍检查发现的链接样式问题已修复，后续混合资料浏览器测试通过。

MNP 读取性能已定位并修复：快照逐文件从“按剩余 1 GiB 预算读取”改为“实际大小 + 1”有界读取，
同时拒绝捕获时文件增长/缩短。原摘要、闭合集合及语义验证全部保留，未缓存可变目录验证结论。
相关 19 项测试通过；MNP 浏览器按原 120 秒结果等待门限读取到 1 个对象并完成来源回查，
实际该次浏览器查询约 100.83 秒。独立读取约 46.27 秒，计时条件不同，不用作研究速度指标。

新 Wheel/SDist 已构建；旧命名空间只含三个薄入口，不包含第二份实现。
独立安装导入与仓库外 Wheel/CLI/资源测试分别记录；安装成功不代表正式发行资格。

## 不由上述结果推出的结论

真实 Qwen/BGE/FAISS/reranker 权重推理、OCR/ASR/视频覆盖、独立专家审核准确率、
跨模态识别/可比速度目标、研究四异常准确率/完成率提升、任意自然语言规则等价证明、外部平台部署、
GitHub 远端更名和正式发布资格均需单独证据。
两轮模型复用/补全、受约束抽取和白名单补丁链已有显式 API 替代实现和实际调用证据；不可将替代方法标成原模型/硬件已运行。
不得将本地工程链通过写成“附件所有要求已完成”。
