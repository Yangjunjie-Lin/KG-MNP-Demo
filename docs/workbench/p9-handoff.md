# P9 admission and final consolidation checklist

Admission: **NOT READY**. Resume unfinished Prompt 8 (8A then 8B then 8C);
do not rename it a new Prompt, Stage or Phase and do not claim P9 is complete.

- Complete service-to-core handlers and bounded typed resource APIs: ingestion,
  KG-IR/evidence, Scope/CQ/baseline/terminology, candidate/proposal/prevalidation,
  human review/replay/finalize, confirmed package, compile/validate/export,
  registry import, diff/impact/regression, release and environment transitions,
  released OMS/ODS. Fence core state commits, not just job result updates.
- Complete same-origin opaque HttpOnly sessions, credential rotation/revocation,
  CSRF/Origin controls, response types and generated frontend client.
- Build one Chinese domain-neutral frontend and its production packaging;
  perform required real browser, accessibility, performance and screenshot QA.
- Implement authorized forestry 0.2.0 synthetic assets/lock/CQ/oracles; retain
  minimal/mnp byte identity and run the three levels of real workflow evidence
  plus the temporary fourth-pack UI smoke.
- Retained Stage/Phase code and docs: inventory overlapping compiler, modeling,
  authority and lifecycle abstractions without breaking old packages/locks.
- Duplicate tooling/validators: identify exact callers before consolidation;
  do not delete authority or filesystem-confinement assertions for convenience.
- Old entries: all nine files in the legacy migration inventory remain because
  no verified replacement exists. Retire them only after equivalent acceptance.
- Tests/CI: consolidate repeated aggregate execution while preserving unique
  collected-node union, serialized security/concurrency tests, JUnit and hashes.
  Configured CI is not a passed CI run.
- Dependencies/release: reconcile explicit preparation and locked builds;
  build/install the real Workbench into wheel/distribution and test a clean
  installation, security scans, startup/shutdown and single-origin deep links.
- Final full-chain acceptance: resolve all Prompt 8 NO_GO reasons, preserve
  GraphDB optional external-license block separately, then re-evaluate P9 entry.
