# Registry format v1

`registry-manifest.json`, immutable records, content-addressed package blobs,
an append-only event chain, a CAS head, deterministic snapshots, and derived
indexes form the project-local registry layout.  `kg-mnp lifecycle registry`
is the supported offline interface.
