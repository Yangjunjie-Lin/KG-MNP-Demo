# Semantic diff v1

The deterministic diff engine compares TBox, ABox, SHACL, mappings, competency
questions, dependencies, annotations, provenance, and metadata.  Raw deltas,
affected IRIs, classification counts, and an overall classification are bound
to the report digest.
