# Dependency and impact analysis

Dependency graph snapshots are deterministic projections of registered
consumers.  Impact reports identify affected terms, mappings, queries,
consumers, environments, and required migrations without mutating packages.
