# Breaking-change classification

Classification rules distinguish no change, annotation-only, patch-compatible,
relaxing, data, additive, potentially breaking, breaking, and unknown/review
required changes.  Unknown constructs are never silently treated as safe.
