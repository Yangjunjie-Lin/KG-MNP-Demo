# Registry events and snapshots

Events bind sequence, previous hash, semantic event hash, event hash, and UTC
observation time.  Replay rejects deletion, reorder, injection, duplicate
semantic events, head tampering, and invalid immutable records.
