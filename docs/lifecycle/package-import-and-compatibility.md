# Package import and compatibility

Directory and `.kgop` imports reuse the Prompt 5 verifier and canonical archive
writer.  Package, lock, archive, name/version, version-IRI, and historical
contract compatibility checks complete before a registry record or event is
committed.
