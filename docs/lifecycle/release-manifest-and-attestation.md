# Release manifest and attestation

Publication produces an immutable `RELEASED` manifest only after human review.
The attestation binds release, package, lock, archive, registry-head, review,
diff, impact, and regression evidence.
