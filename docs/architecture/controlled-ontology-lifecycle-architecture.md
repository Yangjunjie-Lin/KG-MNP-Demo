# Controlled ontology lifecycle architecture

The lifecycle is a local, content-addressed control plane around the existing
Prompt 5 verifier and canonicalization profile.  Authority flows from package
verification through immutable registry evidence, semantic analysis, human
review, release attestation, and environment CAS selection.
